<div class="col-md-12">
  <br><br>
  <div class="pull-right">
      <ul class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown">
          <li class="btn btn-default">排序
            <b class="caret"></b>
          </li>
        </a>
        <ul class="dropdown-menu" aria-labelledby="dropdwonMenuDivider">
          <li><%= link_to("按照薪资下限排序", jobs_path(:order => "by_lower_bound")) %></li>
          <li><%= link_to("按照薪资上限排序", jobs_path(:order => "by_upper_bound")) %></li>
          <li><%= link_to("按照发表时间排序", jobs_path(:order => "by_created_at")) %></li>
        </ul>
      </ul>
  </div>

  <br>

  <table class="table table-boldered table-striped custab">
    <thead>
      <tr>
        <th> Title </th>
        <th> Wage Upper Bound </th>
        <th> Wage Lower Bound </th>
        <th> Post Date </th>
      </tr>
    </thead>

    <tbody>
      <%= render :partial => "job_item", :collection => @jobs, :as => :job %>
    </tbody>
  </table>

  <div class="text-center">
    <%= will_paginate @jobs %>
  </div>
</div>
